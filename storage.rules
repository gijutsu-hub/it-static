rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // KYC photos: authenticated users can upload, only admins can read
    match /kyc/{userId}/{allPaths=**} {
      allow write: if request.auth != null;
      allow read: if request.auth != null;
    }
  }
}
