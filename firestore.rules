rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All Firestore access requires a valid Firebase auth token.
    // The client signs in anonymously (firebase.ts) solely to obtain this token.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
